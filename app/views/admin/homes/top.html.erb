<div class="container">
  <div class="row">
    <div class="col-xs-12">
      <h1>管理者画面</h1>
      <h2>注文一覧</h2>
      購入日
      購入者
      注文数
      注文ステータス
      
      
      <% @order.each do |order| %>
        <% link_to admin_order_path(order) %>
          <%= order.created_at %>
        <% end %>
        
        <%= order.customer.last_name %>
      
      
      <% end %>
      
      
（範囲、ハッシュ、配列などを指定）.each do |変数|
  実行する処理
end
    </div>
  </div>
</div>